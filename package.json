{
  "name": "consulta-horas",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy public --project-name hesm-horas",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
